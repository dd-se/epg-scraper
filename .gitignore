node_modules/
*.log

# scraper output artifacts
epg_*.xml
epg_*.xml.gz

# local scratch / fetched reference files
.tmp/

# local Spor Ekranı API protocol notes (may contain credentials — never commit)
sporekrani-api.md

# local environment files (may hold credentials — never commit)
.agents
.cline
opencode.json
.env
.env.*
!.env.example